# Exam Invigilation System

Python project for exam invigilation using face/head-pose detection and audio monitoring.

Contents:
- `src/` — main source files
- `unit_test/` — helper/process scripts

To upload: ensure `git` is installed and authenticated with GitHub, then run `git push`.
